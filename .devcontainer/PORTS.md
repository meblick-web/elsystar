# ELSYSTAR Codespaces ports

User-facing preview ports:

- `6300` — ELSYSTAR Public Site
- `6301` — ELSYSTAR Admin

Internal Next.js ports:

- `16300` — web upstream
- `16301` — admin upstream

The internal ports are implementation details of the Codespaces reverse-proxy and are configured not to auto-forward.
