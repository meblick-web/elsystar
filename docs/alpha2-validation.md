# Alpha 2 validation

This file exists to trigger pull-request CI for the `v0.1.0-alpha.2` foundation after the initial direct-to-main bootstrap phase.

Validation target:

- npm workspace install
- Prisma client generation
- TypeScript checks for public and admin apps
- production builds for public and admin apps
